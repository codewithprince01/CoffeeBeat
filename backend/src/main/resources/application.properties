# Server Configuration
server.port=8081
# server.servlet.context-path=/api

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://codewithprince0_db_user:coffeebeat123@cluster0.v3gyanj.mongodb.net/coffee-beat?retryWrites=true&w=majority
spring.data.mongodb.database=coffee-beat

# Logging Configuration
logging.level.com.coffeebeat=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.mongodb=DEBUG

# JWT Configuration
jwt.secret=mySuperSecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLongForHS256AlgorithmSecurityRequirement
jwt.expiration=86400000
jwt.refresh-secret=your-super-secret-refresh-key-change-in-production
jwt.refresh-expiration=604800000

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
app.upload.dir=uploads

# CORS Configuration
cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://localhost:5174
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
